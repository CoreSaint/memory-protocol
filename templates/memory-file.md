---
description: Template for a managed canonical or reference memory Markdown file.
---

# Focused title

When copying, retain frontmatter with one non-empty `description` and replace the title. State only durable, scoped knowledge. Link to adjacent details with the declared template placeholder `[[path/to/file.md]]`.
