---
description: Compact always-read index of project-specific memory directories and their entry points.
---

# Project index

Add one real-name directory per project, for example `system/projects/example-service/overview.md`. Keep only project names, paths, and links here; retain detailed architecture in `reference/`.
