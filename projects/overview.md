---
description: On-demand index of project-specific memory directories and their entry points.
---

# Project index

Load this index only when project context is relevant. Add one real-name directory per project, for example `projects/example-service/overview.md`. Keep each project overview compact; retain detailed architecture in `reference/`.
