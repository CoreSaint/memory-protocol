---
description: Contract for reusable agent-owned procedures stored with portable memory.
---

# Skills

Each skill lives at `skills/<skill-name>/SKILL.md`. Its frontmatter must include a `name` matching `<skill-name>` and a non-empty `description`. A skill contains a repeatable procedure, not enduring project facts; store those in `system/` or `reference/` instead.
