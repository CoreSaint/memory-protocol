---
description: Compact navigation that bootstrap or a declared native adapter must load explicitly.
---

# Memory overview

The `AGENT_MEMORY.md` bootstrap procedure—or an adapter that declares and attests native injection—must load this compact set. Placement in `system/` alone has no effect.

Always-read files:

- [[system/persona.md]] — agent identity and durable working principles.
- [[system/memory-policy.md]] — selected permission model for memory writes.
- [[system/human/overview.md]] — compact navigation to consented owner preferences.

Canonical private memory is committed Markdown under `system/`, `projects/`, `reference/`, and `skills/`; project-specific memory loads only after identifying the relevant project. Inspect [[inbox/README.md]] artifacts and attached repositories only as separately labeled, progressive sources. They are not part of the canonical private corpus.

Committed edits affect future bootstrap or native recompilation, not an already-compiled prompt turn.
